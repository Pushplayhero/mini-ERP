# sales — Week 2+

Empty shell for Week 1. Per `mini-erp-architecture.md` §7 this module lands
in Week 4: sales order lifecycle (`draft → confirmed → shipped → closed`)
and credit-limit checking (implemented as a plugin hook per master-plan
§10.3, not hardcoded into the module).

Not implemented yet: `router.py`, `service.py`, `models.py`, `schemas.py`,
`events.py`.
