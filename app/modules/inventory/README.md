# inventory — Week 2+

Empty shell for Week 1. Per `mini-erp-architecture.md` §7 this module lands
in Week 5: append-only stock-move ledger, shipment stock deduction, and the
`stock_summary` + `SELECT ... FOR UPDATE` concurrency control described in
master-plan §10.5.

Not implemented yet: `router.py`, `service.py`, `models.py`, `schemas.py`,
`events.py`.
